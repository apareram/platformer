{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Jugar / Debuggear (LLDB)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/game_executable",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",  // <--- ESTA ES LA CLAVE MÁGICA
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "Compilar Juego (SDL3)" // Asegúrate de que este nombre coincida con el "label" de tu tasks.json
        }
    ]
}